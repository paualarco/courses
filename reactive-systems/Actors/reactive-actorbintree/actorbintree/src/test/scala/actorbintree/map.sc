import actorbintree.BinaryTreeSet.{Contains, Operation}

import scala.collection.immutable.Queue

var map = Map[String, Tuple2[Int, Int]]()


map.size

map.isEmpty

var pendingQueue = Queue.empty[Int]

pendingQueue= pendingQueue ++ Queue(1)
pendingQueue= pendingQueue ++ Queue(2)

pendingQueue= pendingQueue ++ Queue(3)


pendingQueue.tail

pendingQueue.foreach(println)

